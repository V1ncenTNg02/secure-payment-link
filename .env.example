# Supabase PostgreSQL — copy from your project's Settings → Database
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@<host>:6543/postgres

DB_HOST=<host>
DB_PORT=6543
DB_NAME=postgres
DB_USER=postgres.<project-ref>
DB_PASSWORD=<password>
